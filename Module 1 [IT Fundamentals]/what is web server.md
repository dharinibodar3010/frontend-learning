# What is Web Server?
- A web server is a system that stores and delivers web pages to users over the internet.
- It handles requests from browsers and sends the requested data.

**Example**
- Apache HTTP Server is a popular web server.