/*
Splitting utility functions into separate files
makes the project easier to manage and maintain.

Each file can handle a specific type of functionality,
so the code becomes cleaner as the application grows.

For example, a Zomato-like app can keep payment
functions in paymentUtils.js and restaurant functions
in restaurantUtils.js.

These functions can then be imported wherever they
are needed without writing the same code again.
*/